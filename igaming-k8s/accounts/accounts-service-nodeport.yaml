apiVersion: v1
kind: Service
metadata:
  name: accounts-service-nodeport
  namespace: accounts
spec:
  type: NodePort
  selector:
    app: accounts-service
  ports:
  - protocol: TCP
    port: 8091
    targetPort: 8091
    nodePort: 30091

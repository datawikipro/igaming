apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aggregator-postgres-pvc
  namespace: igaming-dev
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path

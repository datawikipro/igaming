apiVersion: v1
kind: Service
metadata:
  name: igaming-source-betboom-crawler
  namespace: igaming-source
spec:
  selector:
    app: igaming-source-betboom-crawler
  ports:
  - protocol: TCP
    port: 3037
    targetPort: 3037
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: igaming-source-betboom-crawler
  namespace: igaming-source
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: igaming-source-betboom-crawler
  template:
    metadata:
      labels:
        app: igaming-source-betboom-crawler
    spec:
      imagePullSecrets:
      - name: regcred
      dnsConfig:
        options:
        - name: ndots
          value: "1"
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 10
            preference:
              matchExpressions:
              - key: node-type
                operator: In
                values:
                - standard
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: node-type
                operator: In
                values:
                - spot
                - standard
                - master
      initContainers:
      - name: db-schema-check
        image: postgres:15-alpine
        imagePullPolicy: IfNotPresent
        env:
        - name: PGPASSWORD
          value: postgres
        command:
        - /bin/sh
        - -c
        - |
          echo "🔍 Checking database schema readiness..."
          HOST="igaming-source-betboom-db"
          DB="igaming_betboom"
          USER="postgres"
          export PGPASSWORD="postgres"

          MAX_RETRIES=5
          COUNT=0
          until nc -z -v -w 3 "$HOST.igaming-source.svc.cluster.local." 5432 >/dev/null 2>&1 || [ $COUNT -ge $MAX_RETRIES ]; do
            echo "Waiting for PostgreSQL ($HOST) to accept queries..."
            sleep 2
            COUNT=$((COUNT + 1))
          done

          EXISTS=$(psql -h "$HOST.igaming-source.svc.cluster.local." -U "$USER" -d "$DB" -tAc "SELECT EXISTS (SELECT FROM information_schema.tables WHERE table_name = 'match_cache');" 2>/dev/null | tr -d '[:space:]')

          if [ "$EXISTS" = "t" ]; then
            echo "✅ Database tables already exist. Fast-starting application with ddl-auto=none..."
            exit 0
          fi

          echo "⚙️ Fresh database detected! Initializing schema tables..."
          psql -h "$HOST.igaming-source.svc.cluster.local." -U "$USER" -d "$DB" -c "
          CREATE TABLE IF NOT EXISTS match_cache (
              id BIGSERIAL PRIMARY KEY,
              external_id VARCHAR(255) NOT NULL UNIQUE,
              bookmaker VARCHAR(50),
              sport_id INT,
              sport_name VARCHAR(255),
              league_name VARCHAR(255),
              format_info VARCHAR(255),
              team1 VARCHAR(255),
              team2 VARCHAR(255),
              score1 VARCHAR(255),
              score2 VARCHAR(255),
              is_live BOOLEAN DEFAULT FALSE,
              start_time BIGINT,
              potential_end_time BIGINT,
              event_url VARCHAR(1024),
              status VARCHAR(20) DEFAULT 'NEW',
              json_payload TEXT,
              payload_hash VARCHAR(64),
              updated_at TIMESTAMP,
              created_at TIMESTAMP,
              last_changed_at TIMESTAMP,
              external_updated_at BIGINT,
              version INT DEFAULT 0
          );
          CREATE TABLE IF NOT EXISTS league_cache (
              id BIGSERIAL PRIMARY KEY,
              external_id VARCHAR(255) NOT NULL UNIQUE,
              sport_name VARCHAR(255),
              region_name VARCHAR(255),
              league_name VARCHAR(255),
              url VARCHAR(1024),
              status VARCHAR(50),
              created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
              updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
          );
          CREATE TABLE IF NOT EXISTS sport_cache (
              id BIGSERIAL PRIMARY KEY,
              external_id INT UNIQUE NOT NULL,
              name VARCHAR(255),
              kind VARCHAR(255),
              parent_id INT,
              updated_at TIMESTAMP
          );
          CREATE TABLE IF NOT EXISTS unmapped_bet (
              id BIGSERIAL PRIMARY KEY,
              sport_name VARCHAR(255),
              raw_name VARCHAR(255) NOT NULL,
              group_name VARCHAR(255),
              event_id VARCHAR(255),
              created_at TIMESTAMP
          );
          CREATE INDEX IF NOT EXISTS idx_mc_status_updated ON match_cache(status, updated_at);
          CREATE INDEX IF NOT EXISTS idx_mc_is_live ON match_cache(is_live);
          CREATE INDEX IF NOT EXISTS idx_mc_last_changed ON match_cache(last_changed_at);
          CREATE INDEX IF NOT EXISTS idx_mc_bookmaker ON match_cache(bookmaker);
          CREATE INDEX IF NOT EXISTS idx_mc_sport_live ON match_cache(sport_name, is_live);
          CREATE INDEX IF NOT EXISTS idx_mc_potential_end_time ON match_cache(potential_end_time);
          "
          echo "✅ Database schema initialization completed successfully!"
        resources:
          requests:
            cpu:  20m
            memory:  256Mi
          limits:
            cpu:  1200m
            memory:  1280Mi
      containers:
      - name: igaming-source-betboom-crawler
        image: ghcr.io/datawikipro/igaming-source-betboom:latest
        imagePullPolicy: Always
        command: ["/bin/sh", "-c"]
        args:
        - |
          Xvfb :99 -ac -screen 0 1920x1080x24 -nolisten tcp &
          sleep 2
          export DISPLAY=:99
          exec java -jar app.jar
        env:
        - name: APP_BROWSER_STEALTH_PROFILE
          value: XVFB_HEADED
        - name: SPRING_PROFILES_ACTIVE
          value: dev,league-crawler
        - name: APP_ROLE
          value: league-crawler
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://igaming-source-betboom-db.igaming-source.svc.cluster.local:5432/igaming_betboom
        - name: SPRING_DATASOURCE_HIKARI_INITIALIZATION_FAIL_TIMEOUT
          value: "0"
        - name: SPRING_DATASOURCE_HIKARI_CONNECTION_TIMEOUT
          value: "5000"
        - name: SPRING_DATASOURCE_HIKARI_VALIDATION_TIMEOUT
          value: "3000"
        - name: SPRING_JPA_PROPERTIES_HIBERNATE_TEMP_USE_JDBC_METADATA_DEFAULTS
          value: "false"
        - name: SPRING_JPA_DATABASE_PLATFORM
          value: org.hibernate.dialect.PostgreSQLDialect
        - name: SPRING_DATASOURCE_USERNAME
          value: postgres
        - name: SPRING_DATASOURCE_PASSWORD
          value: postgres
        - name: APP_AGGREGATOR_URL
          value: http://igaming-aggregator.igaming-master.svc.cluster.local
        - name: BETBOOM_API_BASE_URL
          value: https://siteapi.betboom.ru/api/site_api/v1/sporthub
        - name: BETBOOM_SPORTS_URL
          value: https://siteapi.betboom.ru/api/site_api/v1/sporthub/tree/get_tree
        - name: BETBOOM_EVENTS_URL
          value: https://siteapi.betboom.ru/api/site_api/v1/sporthub/tree/get_match_details_info
        - name: BETBOOM_WS_URL
          value: wss://ru-ws.sporthub.bet:444/api/tree_ws/v1
        - name: SPRING_DATA_REDIS_HOST
          value: localhost
        - name: APP_PERSISTENCE_USE_REDIS_FACTORS
          value: 'true'
        - name: APP_PROXY_BACKEND_URL
          value: http://service-proxy-backend.service-proxy.svc.cluster.local
        - name: APP_BETBOOM_FETCH_INTERVAL_SECONDS
          value: '144'
        - name: APP_BETBOOM_PREMATCH_FETCH_INTERVAL_SECONDS
          value: '720'
        - name: APP_ODDS_REFRESH_LIVE_POLL_MS
          value: '144000'
        - name: APP_ODDS_REFRESH_PREMATCH_POLL_MS
          value: '720000'
        - name: APP_MATCH_LOADER_POLL_DELAY_MS
          value: '3600'
        - name: SERVER_PORT
          value: '3037'
        resources:
          requests:
            cpu:  20m
            memory:  256Mi
          limits:
            cpu:  1200m
            memory:  1280Mi
        ports:
        - containerPort: 3037
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        resources:
          requests:
            cpu: 5m
            memory: 32Mi
          limits:
            cpu: 50m
            memory: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: igaming-source-betboom-loader
  namespace: igaming-source
spec:
  selector:
    app: igaming-source-betboom-loader
  ports:
  - protocol: TCP
    port: 3037
    targetPort: 3037
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: igaming-source-betboom-loader
  namespace: igaming-source
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: igaming-source-betboom-loader
  template:
    metadata:
      labels:
        app: igaming-source-betboom-loader
    spec:
      imagePullSecrets:
      - name: regcred
      dnsConfig:
        options:
        - name: ndots
          value: "1"
      initContainers:
      - name: db-schema-check
        image: postgres:15-alpine
        imagePullPolicy: IfNotPresent
        env:
        - name: PGPASSWORD
          value: postgres
        command:
        - /bin/sh
        - -c
        - |
          echo "🔍 Checking database schema readiness..."
          HOST="igaming-source-betboom-db"
          DB="igaming_betboom"
          USER="postgres"
          export PGPASSWORD="postgres"

          MAX_RETRIES=5
          COUNT=0
          until nc -z -v -w 3 "$HOST.igaming-source.svc.cluster.local." 5432 >/dev/null 2>&1 || [ $COUNT -ge $MAX_RETRIES ]; do
            echo "Waiting for PostgreSQL ($HOST) to accept queries..."
            sleep 2
            COUNT=$((COUNT + 1))
          done

          EXISTS=$(psql -h "$HOST.igaming-source.svc.cluster.local." -U "$USER" -d "$DB" -tAc "SELECT EXISTS (SELECT FROM information_schema.tables WHERE table_name = 'match_cache');" 2>/dev/null | tr -d '[:space:]')

          if [ "$EXISTS" = "t" ]; then
            echo "✅ Database tables already exist. Fast-starting application with ddl-auto=none..."
            exit 0
          fi

          echo "⚙️ Fresh database detected! Initializing schema tables..."
          psql -h "$HOST.igaming-source.svc.cluster.local." -U "$USER" -d "$DB" -c "
          CREATE TABLE IF NOT EXISTS match_cache (
              id BIGSERIAL PRIMARY KEY,
              external_id VARCHAR(255) NOT NULL UNIQUE,
              bookmaker VARCHAR(50),
              sport_id INT,
              sport_name VARCHAR(255),
              league_name VARCHAR(255),
              format_info VARCHAR(255),
              team1 VARCHAR(255),
              team2 VARCHAR(255),
              score1 VARCHAR(255),
              score2 VARCHAR(255),
              is_live BOOLEAN DEFAULT FALSE,
              start_time BIGINT,
              potential_end_time BIGINT,
              event_url VARCHAR(1024),
              status VARCHAR(20) DEFAULT 'NEW',
              json_payload TEXT,
              payload_hash VARCHAR(64),
              updated_at TIMESTAMP,
              created_at TIMESTAMP,
              last_changed_at TIMESTAMP,
              external_updated_at BIGINT,
              version INT DEFAULT 0
          );
          CREATE TABLE IF NOT EXISTS league_cache (
              id BIGSERIAL PRIMARY KEY,
              external_id VARCHAR(255) NOT NULL UNIQUE,
              sport_name VARCHAR(255),
              region_name VARCHAR(255),
              league_name VARCHAR(255),
              url VARCHAR(1024),
              status VARCHAR(50),
              created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
              updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
          );
          CREATE TABLE IF NOT EXISTS sport_cache (
              id BIGSERIAL PRIMARY KEY,
              external_id INT UNIQUE NOT NULL,
              name VARCHAR(255),
              kind VARCHAR(255),
              parent_id INT,
              updated_at TIMESTAMP
          );
          CREATE TABLE IF NOT EXISTS unmapped_bet (
              id BIGSERIAL PRIMARY KEY,
              sport_name VARCHAR(255),
              raw_name VARCHAR(255) NOT NULL,
              group_name VARCHAR(255),
              event_id VARCHAR(255),
              created_at TIMESTAMP
          );
          CREATE INDEX IF NOT EXISTS idx_mc_status_updated ON match_cache(status, updated_at);
          CREATE INDEX IF NOT EXISTS idx_mc_is_live ON match_cache(is_live);
          CREATE INDEX IF NOT EXISTS idx_mc_last_changed ON match_cache(last_changed_at);
          CREATE INDEX IF NOT EXISTS idx_mc_bookmaker ON match_cache(bookmaker);
          CREATE INDEX IF NOT EXISTS idx_mc_sport_live ON match_cache(sport_name, is_live);
          CREATE INDEX IF NOT EXISTS idx_mc_potential_end_time ON match_cache(potential_end_time);
          "
          echo "✅ Database schema initialization completed successfully!"
        resources:
          requests:
            cpu:  20m
            memory:  256Mi
          limits:
            cpu:  800m
            memory:  512Mi
      containers:
      - name: igaming-source-betboom-loader
        image: ghcr.io/datawikipro/igaming-source-betboom:latest
        imagePullPolicy: Always
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: dev,match-loader
        - name: APP_ROLE
          value: match-loader
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://igaming-source-betboom-db.igaming-source.svc.cluster.local:5432/igaming_betboom
        - name: SPRING_DATASOURCE_HIKARI_INITIALIZATION_FAIL_TIMEOUT
          value: "0"
        - name: SPRING_DATASOURCE_HIKARI_CONNECTION_TIMEOUT
          value: "5000"
        - name: SPRING_DATASOURCE_HIKARI_VALIDATION_TIMEOUT
          value: "3000"
        - name: SPRING_JPA_PROPERTIES_HIBERNATE_TEMP_USE_JDBC_METADATA_DEFAULTS
          value: "false"
        - name: SPRING_JPA_DATABASE_PLATFORM
          value: org.hibernate.dialect.PostgreSQLDialect
        - name: SPRING_DATASOURCE_USERNAME
          value: postgres
        - name: SPRING_DATASOURCE_PASSWORD
          value: postgres
        - name: APP_AGGREGATOR_URL
          value: http://igaming-aggregator.igaming-master.svc.cluster.local
        - name: BETBOOM_API_BASE_URL
          value: https://siteapi.betboom.ru/api/site_api/v1/sporthub
        - name: BETBOOM_SPORTS_URL
          value: https://siteapi.betboom.ru/api/site_api/v1/sporthub/tree/get_tree
        - name: BETBOOM_EVENTS_URL
          value: https://siteapi.betboom.ru/api/site_api/v1/sporthub/tree/get_match_details_info
        - name: BETBOOM_WS_URL
          value: wss://ru-ws.sporthub.bet:444/api/tree_ws/v1
        - name: SPRING_DATA_REDIS_HOST
          value: localhost
        - name: APP_PERSISTENCE_USE_REDIS_FACTORS
          value: 'true'
        - name: APP_PROXY_BACKEND_URL
          value: http://service-proxy-backend.service-proxy.svc.cluster.local
        - name: APP_BETBOOM_FETCH_INTERVAL_SECONDS
          value: '144'
        - name: APP_BETBOOM_PREMATCH_FETCH_INTERVAL_SECONDS
          value: '720'
        - name: APP_ODDS_REFRESH_LIVE_POLL_MS
          value: '144000'
        - name: APP_ODDS_REFRESH_PREMATCH_POLL_MS
          value: '720000'
        - name: APP_MATCH_LOADER_POLL_DELAY_MS
          value: '3600'
        - name: SERVER_PORT
          value: '3037'
        resources:
          requests:
            cpu:  20m
            memory:  256Mi
          limits:
            cpu:  800m
            memory:  512Mi
        ports:
        - containerPort: 3037
        startupProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 3037
          initialDelaySeconds: 30
          periodSeconds: 15
          failureThreshold: 120
          timeoutSeconds: 5
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 3037
          initialDelaySeconds: 15
          periodSeconds: 15
          failureThreshold: 3
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 3037
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        resources:
          requests:
            cpu: 5m
            memory: 32Mi
          limits:
            cpu: 50m
            memory: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: igaming-source-betboom-db
  namespace: igaming-source
spec:
  selector:
    app: igaming-source-betboom-db
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: igaming-source-betboom-db
  namespace: igaming-source
spec:
  serviceName: igaming-source-betboom-db
  replicas: 1
  selector:
    matchLabels:
      app: igaming-source-betboom-db
  template:
    metadata:
      labels:
        app: igaming-source-betboom-db
    spec:
      nodeSelector:
        node-type: standard
      containers:
      - name: postgres
        image: postgres:15-alpine
        env:
        - name: POSTGRES_DB
          value: igaming_betboom
        - name: POSTGRES_USER
          value: postgres
        - name: POSTGRES_PASSWORD
          value: postgres
        ports:
        - containerPort: 5432
        resources:
          requests:
            cpu: 10m
            memory: 32Mi
          limits:
            cpu: 100m
            memory: 96Mi
        volumeMounts:
        - mountPath: /var/lib/postgresql/data
          name: pgdata
      volumes:
      - name: pgdata
        emptyDir: {}


# DEPRECATED

This service (`antigravity-overlay-backend`) is deprecated and replaced by the new Accounts Platform:
- `accounts-service`: For data storage
- `llm-poller`: For polling `agy.exe` and quota management on Windows.

Do not add new features here.

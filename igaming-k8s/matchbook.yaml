apiVersion: v1
kind: Service
metadata:
  name: igaming-source-matchbook
  namespace: igaming-source
  labels:
    app: igaming-source-matchbook
spec:
  selector:
    app: igaming-source-matchbook
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: igaming-source-matchbook
  namespace: igaming-source
  labels:
    app: igaming-source-matchbook
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: igaming-source-matchbook
  template:
    metadata:
      labels:
        app: igaming-source-matchbook
    spec:
      imagePullSecrets:
      - name: regcred
      dnsConfig:
        options:
        - name: ndots
          value: "1"
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 10
            preference:
              matchExpressions:
              - key: node-type
                operator: In
                values:
                - standard
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: node-type
                operator: In
                values:
                - spot
                - standard
                - master
      initContainers:
      - name: db-wait
        image: postgres:15-alpine
        imagePullPolicy: IfNotPresent
        env:
        - name: PGPASSWORD
          value: postgres
        command:
        - /bin/sh
        - -c
        - |
          echo "Waiting for igaming-source-matchbook-db..."
          until pg_isready -h igaming-source-matchbook-db.igaming-source.svc.cluster.local -p 5432 -U postgres; do
            sleep 2
          done
          echo "Database is ready!"
      containers:
      - name: igaming-source-matchbook
        image: ghcr.io/datawikipro/igaming-source-matchbook:latest
        imagePullPolicy: Always
        env:
        - name: SERVER_PORT
          value: "8080"
        - name: DB_HOST
          value: "igaming-source-matchbook-db"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "matchbook"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          value: "postgres"
        - name: SPRING_DATASOURCE_HIKARI_INITIALIZATION_FAIL_TIMEOUT
          value: "0"
        - name: SPRING_JPA_PROPERTIES_HIBERNATE_TEMP_USE_JDBC_METADATA_DEFAULTS
          value: "false"
        - name: SPRING_JPA_DATABASE_PLATFORM
          value: "org.hibernate.dialect.PostgreSQLDialect"
        - name: HTTP_PROXY_ENABLED
          value: "false"
        - name: MATCHBOOK_API_URL
          value: "https://api.matchbook.com/edge/rest"
        - name: APP_BOOKMAKER_NAME
          value: "matchbook"
        - name: APP_BOOKMAKER_REGIONS
          value: "GB,EU,GLOBAL"
        - name: APP_AGGREGATOR_URL
          value: "http://igaming-aggregator.igaming-dev.svc.cluster.local"
        - name: JAVA_OPTS
          value: "-XX:+UseContainerSupport -XX:MaxRAMPercentage=50.0 -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+UseStringDeduplication"
        resources:
          requests:
            cpu: 10m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 384Mi
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: igaming-source-matchbook-db
  namespace: igaming-source
  labels:
    app: igaming-source-matchbook-db
spec:
  selector:
    app: igaming-source-matchbook-db
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: igaming-source-matchbook-db
  namespace: igaming-source
  labels:
    app: igaming-source-matchbook-db
spec:
  serviceName: igaming-source-matchbook-db
  replicas: 1
  selector:
    matchLabels:
      app: igaming-source-matchbook-db
  template:
    metadata:
      labels:
        app: igaming-source-matchbook-db
    spec:
      nodeSelector:
        node-type: standard
      containers:
      - name: postgres
        image: postgres:15-alpine
        args:
        - "-c"
        - "fsync=off"
        - "-c"
        - "synchronous_commit=off"
        - "-c"
        - "full_page_writes=off"
        - "-c"
        - "wal_level=minimal"
        - "-c"
        - "max_wal_senders=0"
        - "-c"
        - "shared_buffers=128MB"
        env:
        - name: POSTGRES_DB
          value: matchbook
        - name: POSTGRES_USER
          value: postgres
        - name: POSTGRES_PASSWORD
          value: postgres
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        command:
        - docker-entrypoint.sh
        - postgres
        - -c
        - synchronous_commit=off
        - -c
        - shared_buffers=64MB
        - -c
        - max_connections=50
        ports:
        - containerPort: 5432
        resources:
          requests:
            cpu: 10m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 192Mi
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: postgres-data
        emptyDir:
          medium: Memory
          sizeLimit: 1Gi

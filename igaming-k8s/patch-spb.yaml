spec:
  template:
    spec:
      initContainers:
      - name: wait-for-postgres
        command:
        - sh
        - -c
        - until pg_isready -h proxy-postgres.proxy.svc.cluster.local -p 5432 -U postgres; do echo waiting for postgres; sleep 2; done;
